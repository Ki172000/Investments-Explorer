/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // We can add external image hosting domains here later if needed
  },
};

module.exports = nextConfig;